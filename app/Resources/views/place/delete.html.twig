{% extends 'base.html.twig' %}

{% block body %}
    <table class="tabela2">
        <thead>
        <tr>
            <th colspan="2">Czy na pewno chcesz usunąć miejsce: {{ place.name }}</th>
        </tr>
        </thead>
        <tbody>
        <tr>
            <td style="width: 50%"><button><a href="{{ path('place_index', { 'id': place.id }) }}">NIE</a></button></td>
            <td>{{ form_start(delete_form) }}
                <input type="submit" value="TAK" id="buttton">
                {{ form_end(delete_form) }}</td>
        </tr>
        </tbody>
    </table>
{% endblock %}