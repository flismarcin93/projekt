{% extends 'base.html.twig' %}

{% block body %}
    <h1>Kreator tworzenia wydarzeń</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Stwórz" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('event_index') }}">Wróć do listy wydarzeń</a>
        </li>
    </ul>
{% endblock %}
